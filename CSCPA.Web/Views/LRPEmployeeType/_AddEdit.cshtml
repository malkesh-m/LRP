@using CSCPA.Model
@Html.AntiForgeryToken()

@(Html.DevExtreme().Form<LRPEmployeeTypeAddEditModel>
    ().ID("LRPEmployeeTypeAddEditDevForm")
    .ColCount(1)
    .Items(groupItems =>
    {
        groupItems.AddSimpleFor(m => m.ObjectUID).Visible(false);

        groupItems.AddSimpleFor(m => m.Name).IsRequired(true);

        groupItems.AddButton()
        .ButtonOptions(b => b.Text("Save")
        .Type(ButtonType.Success)
        .UseSubmitBehavior(false)
        .OnClick("lrpEmployeeTypeAddEdit.save")
        );
    })
    .FormData(Model)
    )
