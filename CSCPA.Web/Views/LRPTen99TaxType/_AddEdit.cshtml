@using CSCPA.Model
@Html.AntiForgeryToken()

@(Html.DevExtreme().Form<LRPTen99TaxTypeAddEditModel>
    ().ID("LRPTen99TaxTypeAddEditDevForm")
    .ColCount(2)
    .Items(groupItems =>
    {
        groupItems.AddSimpleFor(m => m.ObjectUID).Visible(false);

        groupItems.AddSimpleFor(m => m.Name).IsRequired(true);
        groupItems.AddSimpleFor(m => m.ValueGp);



      groupItems.AddSimpleFor(m => m.DescriptionBp)
      .ColSpan(2)
      .Editor(e => e
      .TextArea()
      .Height(140)
      );
          groupItems.AddSimpleFor(m => m.Description)
      .ColSpan(2)
      .Editor(e => e
      .TextArea()
      .Height(140)
      );

        groupItems.AddButton()
        .ButtonOptions(b => b.Text("Save")
        .Type(ButtonType.Success)
        .UseSubmitBehavior(false)
        .OnClick("lrpTen99TaxTypeAddEdit.save")
        );
    })
    .FormData(Model)
    )
